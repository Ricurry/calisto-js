const Discord = require("discord.js");


// Notifications Off
const notifs_off = new Discord.MessageEmbed()
    .setTitle('Notifications Off!')
    .setDescription(' ')
    .setColor('#fc0d8d');

module.exports = (notifs_off);